import java.util.*;
class BankAccount
{
	{
		 String Name;
	   int AccNum;
	   String AccType;
	   double AccBal;
	   double InitialBal;
	}
 
 String Customer(String N)
 {
 	
 	return(Name);
 }
 
 double initialBal(double x)
  {
  	return(InitialBal);
  }
 double depAmt(double x)
 {
 	double depAmt = y;
 	InitialBal = x;
 	AccBal = x+y;
 	return (AccBal);
 	
 } 
 double withdrawAmt(double x)
 {
 	double withdrawAmt = y;
 	InitialBal = x;
 	AccBal = x- y;
 	return(AccBal);
 	
 }
  }
 
 class BankDetail
 {
 	public static void main (String args [])
 	{
 		BankAccount B1 = new BankAccount();
 		B1.Name(AbC);
 		initialBal1 = B1.initialBal(3000.10);
 		AccBal1 = B1.depAmt(200.20);
 		AccBal2 = B1.withdrawAmt(10.20);
 		System.out.println("Balance after deposit "+ AccBAl1);
 		System.out.println("Balance after withdrawal "+ AccBal2);
 		
 	}
 }
 
